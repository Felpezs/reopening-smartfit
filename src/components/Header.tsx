import Logo from "./Logo";

const Header = () => {
  return (
    <header className="bg-darkGrey w-full h-24 flex items-center justify-center">
      <Logo />
    </header>
  );
};

export default Header;
